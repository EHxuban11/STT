interface:
  display_name: "Build Tauri Locally"
  short_description: "Build, launch, and troubleshoot the Windows Tauri app on this PC."
  default_prompt: "Build or launch Yawning Face STT locally, avoid the raw-debug localhost trap, and report artifact paths, timing, and dependency status."
